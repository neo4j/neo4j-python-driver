Vector data types and utilities are implemented by the ``neo4j.vector`` module.

It provides a set of types corresponding to Cypher's ``VECTOR`` type.

.. Note::

    Storing ``VECTOR`` values in the database requires a server version >= 2025.10, Enterprise Edition.
